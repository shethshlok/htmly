services:
  mcp-html-server:
    build: .
    container_name: htmly-engine
    ports:
      - "6342:3000"
    environment:
      - PORT=3000
      - BASE_URL=https://html.shloksheth.tech
    volumes:
      - ./public:/app/public
    restart: unless-stopped
