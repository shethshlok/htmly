services:
  mcp-html-server:
    build: .
    container_name: htmly-engine
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
    volumes:
      - ./public:/app/public
    restart: unless-stopped
